import styled from 'styled-components';
import {Col, Row} from 'antd';

export const WrapperStyle = styled.div`
  margin-top: 30px;
`;

export const GridStyle = styled(Row)``;

export const ItemStyle = styled(Col)``;

export const HeadStyle = styled.div`
  display: flex;
  align-items: center;
  margin: 20px 0 10px;
`;
